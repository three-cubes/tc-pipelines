[project]
name = "tc-sdlc-bootstrap-consumer"
version = "1.0.0"
requires-python = ">=3.13,<3.14"
dependencies = ["attrs==26.1.0"]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
