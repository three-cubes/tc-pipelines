"""Off-CI local agent tooling for Three Cubes repos.

Currently exposes `agent-token` — mint a short-lived three-cubes-agent GitHub
App installation token from Key Vault (the local complement to the CI
`github-app-token` composite action).
"""
