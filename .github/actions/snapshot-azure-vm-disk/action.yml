name: 'Snapshot Azure VM OS-Disk'
description: |
  Takes an OS-disk snapshot of one or more Azure VMs before a destructive op.
  Use this as the first step in any reusable deploy workflow — the snapshot
  is the rollback window. Skips cleanly with a loud warn if SKIP_SNAPSHOT is
  set (dev-only override).

  Requires a prior wif-azure-login step (the runner identity needs
  `Disk Snapshot Contributor` scoped to the VM's resource group).

  Snapshot naming follows the Three Cubes convention:
    <vm-name>-osdisk-pre-<op-tag>-YYYYMMDD-HHMMSS

  Every snapshot is incremental and carries lifecycle tags. The
  `prune-azure-vm-snapshots` action deletes snapshots whose `tc-expires-at`
  tag has passed.

  Usage:
    - uses: three-cubes/tc-pipelines/.github/actions/snapshot-azure-vm-disk@1c2944c0cca248bd42fa2e56e84fc3d97a9c698b # v1.19.11
      with:
        resource-group: RG-AGENTS-CORE
        vm-names: 'vm-openclaw,vm-hermes-poc'
        op-tag: deploy-on-merge
        skip:           {{ github.event.inputs.skip_snapshot }}
  (replace {{ ... }} with the literal $-prefixed GitHub-Actions expression
  syntax — de-templated in this docstring so the action parser doesn't
  try to evaluate the example.)

inputs:
  resource-group:
    description: Azure resource group containing the VM(s).
    required: true
  vm-names:
    description: Comma-separated list of VM names to snapshot.
    required: true
  op-tag:
    description: Short tag identifying the operation (e.g. apply-config, deploy-on-merge). Becomes part of the snapshot name.
    required: true
  skip:
    description: |
      Set to "true" to skip snapshot — dev-only override. Logs a warn; never silently skips.
      Useful for the first WIF smoke test before role assignments propagate.
    required: false
    default: 'false'
  retention-hours:
    description: Snapshot recovery window in whole hours. The action writes the expiry as the tc-expires-at tag for the scheduled pruner.
    required: false
    default: '48'

outputs:
  snapshot-names:
    description: Newline-separated list of snapshot resource names created.
    value: ${{ steps.snap.outputs.snapshot-names }}
  snapshot-resource-ids:
    description: Newline-separated Azure resource IDs for snapshots created.
    value: ${{ steps.snap.outputs.snapshot-resource-ids }}
  snapshot-expires-at:
    description: UTC expiry timestamp applied to every snapshot created by this invocation.
    value: ${{ steps.snap.outputs.snapshot-expires-at }}

runs:
  using: composite
  steps:
    - name: Take snapshots
      id: snap
      shell: bash
      env:
        RG: ${{ inputs.resource-group }}
        VMS: ${{ inputs.vm-names }}
        OP: ${{ inputs.op-tag }}
        SKIP: ${{ inputs.skip }}
        RETENTION_HOURS: ${{ inputs.retention-hours }}
      run: |
        set -euo pipefail
        if [[ ! "$RETENTION_HOURS" =~ ^[1-9][0-9]*$ ]]; then
          echo "::error::retention-hours must be a positive whole number of hours"
          exit 1
        fi
        if [[ "${SKIP}" == "true" ]]; then
          echo "::warning::SKIP_SNAPSHOT=true — refusing to snapshot. Dev override only; production deploys MUST snapshot."
          echo "snapshot-names=" >>"$GITHUB_OUTPUT"
          echo "snapshot-resource-ids=" >>"$GITHUB_OUTPUT"
          echo "snapshot-expires-at=" >>"$GITHUB_OUTPUT"
          exit 0
        fi
        STAMP=$(date -u +%Y%m%d-%H%M%S)
        CREATED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)
        EXPIRES_AT=$(date -u -d "+${RETENTION_HOURS} hours" +%Y-%m-%dT%H:%M:%SZ)
        NAMES=""
        IDS=""
        publish_outputs() {
          {
            echo "snapshot-names<<EOF"
            echo "${NAMES}"
            echo "EOF"
            echo "snapshot-resource-ids<<EOF"
            echo "${IDS}"
            echo "EOF"
            echo "snapshot-expires-at=${EXPIRES_AT}"
          } >>"$GITHUB_OUTPUT"
        }
        trap publish_outputs EXIT
        IFS=',' read -ra VM_LIST <<< "${VMS}"
        for vm in "${VM_LIST[@]}"; do
          vm=$(echo "$vm" | xargs)   # trim whitespace
          [[ -z "$vm" ]] && continue
          OSDISK_ID=$(az vm show -g "$RG" -n "$vm" \
            --query 'storageProfile.osDisk.managedDisk.id' -o tsv)
          if [[ -z "$OSDISK_ID" ]]; then
            echo "::error::Could not resolve OS disk for ${vm} in ${RG}. Check VM exists + identity has Reader on RG."
            exit 1
          fi
          NAME="${vm}-osdisk-pre-${OP}-${STAMP}"
          echo "Creating snapshot: ${NAME}"
          SNAPSHOT_JSON=$(az snapshot create \
            -g "$RG" \
            -n "$NAME" \
            --source "$OSDISK_ID" \
            --incremental true \
            --tags \
              "tc-managed-by=tc-pipelines" \
              "tc-purpose=pre-deploy-recovery" \
              "tc-source-vm=${vm}" \
              "tc-operation=${OP}" \
              "tc-created-at=${CREATED_AT}" \
              "tc-expires-at=${EXPIRES_AT}" \
            --query '{state:provisioningState,id:id}' -o json)
          STATE=$(jq -r '.state' <<<"$SNAPSHOT_JSON")
          SNAPSHOT_ID=$(jq -r '.id' <<<"$SNAPSHOT_JSON")
          if [[ "$STATE" != "Succeeded" ]]; then
            echo "::error::Snapshot ${NAME} did not reach Succeeded (state: ${STATE})"
            exit 1
          fi
          if [[ ! "$SNAPSHOT_ID" =~ ^/subscriptions/[^/]+/resourceGroups/[^/]+/providers/Microsoft.Compute/snapshots/[^/]+$ ]]; then
            echo "::error::Snapshot ${NAME} returned an invalid resource ID"
            exit 1
          fi
          NAMES="${NAMES}${NAME}"$'\n'
          IDS="${IDS}${SNAPSHOT_ID}"$'\n'
        done
